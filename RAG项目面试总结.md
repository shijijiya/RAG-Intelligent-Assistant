# RAG项目面试总结

激活环境

conda activate heimaRAG

进入项目目录（必须在 RAG项目 下运行，因为用的是相对路径）

cd "e:\work\projects\RAG问答助手\RAG项目"

设置 DashScope API Key（一次性）

$env:DASHSCOPE_API_KEY = "<your-dashscope-api-key>"

streamlit run app_qa.py
streamlit run app_file_uploader.py
python rag.py                  # 跑问答链
python view_vectors.py         # 查看向量库内容
python vector_stores.py        # 单独测检索

## 1. 项目一句话介绍

这是一个基于 LangChain + DashScope Embedding + Chroma + 通义千问 的轻量级 RAG 问答项目。

它包含两条核心链路：

1. 离线建库流程：把上传的文本切分、向量化并持久化到 Chroma。
2. 在线问答流程：用户提问后先检索知识库，再把检索结果和历史对话一起交给大模型生成答案。

你在面试里可以先用这句话开场：

> 我这个项目本质上是一个带会话记忆的本地持久化 RAG 系统。离线阶段负责知识入库，在线阶段负责检索增强生成，目标是减少大模型幻觉，让回答尽量基于私有知识库。

---

## 2. 项目整体架构

从代码实现上看，主要由 5 部分组成：

1. `knowledge_base.py`
   负责离线入库，包括文本切分、MD5 去重、向量化、写入 Chroma。
2. `vector_stores.py`
   负责封装向量库，并暴露 retriever 给问答链使用。
3. `rag.py`
   负责在线问答主链路：检索、拼 prompt、加载历史消息、调用大模型。
4. `file_history_store.py`
   负责会话记忆，把聊天记录按 `session_id` 落盘到本地文件。
5. `app_file_uploader.py` 和 `app_qa.py`
   负责用 Streamlit 提供上传知识库和聊天问答页面。

你可以把它概括成：

> 前台是两个 Streamlit 页面，一个负责上传知识，一个负责问答；后台分成知识入库、向量检索、RAG 生成、历史记忆四层。

---

## 3. 离线流程如何实现

这是最常被问的部分之一。

### 3.1 离线流程总览

离线流程就是“知识入库流程”，入口在 `app_file_uploader.py`：

1. 用户上传 txt 文件。
2. 文件内容被读取并解码成字符串。
3. 调用 `KnowledgeBaseService.upload_by_str(text, file_name)`。
4. 对文本内容计算 MD5，做重复检测。
5. 如果文本过长，则按设定规则切分成多个 chunk。
6. 调用 embedding 模型把 chunk 向量化。
7. 将文本块和 metadata 一起写入 Chroma。
8. Chroma 持久化到本地目录 `./chroma_db`。

对应代码位置：

- 上传入口：`app_file_uploader.py`
- 入库主逻辑：`knowledge_base.py:68`
- Chroma 持久化目录配置：`config_data.py:6-7`

### 3.2 文本切分如何实现

文本切分使用的是 `RecursiveCharacterTextSplitter`，定义在 `knowledge_base.py:61`。

关键参数：

- `chunk_size = 1000`
- `chunk_overlap = 100`
- `separators = [...]`

含义：

1. 每个 chunk 最多约 1000 个字符。
2. 相邻 chunk 保留 100 个字符重叠。
3. 优先按段落、换行、标点等切分，尽量避免粗暴截断。

面试回答可以这么说：

> 我采用了递归字符切分器。这样做的目的是在控制单块长度的同时，尽量保持语义完整；chunk overlap 主要是避免关键信息刚好落在切分边界导致召回效果下降。

### 3.3 为什么需要切分

面试官经常会追问这个点。

原因主要有 4 个：

1. 原始文档太长，不能整篇直接送进 embedding 和大模型。
2. 检索的最小粒度应该更细，否则召回结果不够精确。
3. 小块文本更适合做向量相似度搜索。
4. 能降低 prompt token 消耗。

### 3.4 向量化和存储如何实现

在 `knowledge_base.py:55-59` 中，项目初始化了 Chroma：

- 向量库：`Chroma`
- embedding 模型：`DashScopeEmbeddings(model="text-embedding-v4")`
- 持久化目录：`./chroma_db`

真正写入发生在 `knowledge_base.py:88-92` 的 `self.chroma.add_texts(...)`。

写入时除了文本块本身，还保存了 metadata：

- `source`
- `create_time`
- `operator`

这意味着面试时你可以强调：

> 我的知识库不只存正文，也存来源文件名和创建时间等元数据。这样后续做溯源展示、过滤检索、审计追踪都会更方便。

### 3.5 离线流程里的去重是怎么做的

这个项目用了一个非常适合初学项目讲清楚的思路：MD5 去重。

实现位置：

- 计算 MD5：`knowledge_base.py:36-47`
- 检查是否已存在：`knowledge_base.py:13-27`
- 保存 MD5：`knowledge_base.py:30-33`
- 使用逻辑：`knowledge_base.py:71-75`、`knowledge_base.py:95`

流程是：

1. 对整个上传文本计算 MD5。
2. 到 `md5.text` 文件里查这个 MD5 是否已经出现过。
3. 如果已经存在，则不重复入库。
4. 如果不存在，则继续切分、向量化、入库，并把 MD5 追加保存。

面试时可以这样回答：

> 我在离线建库阶段做了基于文本内容的 MD5 去重。这样可以避免同一份文档被重复上传，减少向量库冗余数据，也能降低重复 embedding 的成本。

同时你也要知道它的局限：

1. 它是“全文级去重”，不是“chunk 级去重”。
2. 只要内容稍微改动，MD5 就完全不同。
3. 不适合做语义级近重复检测。

如果被问“怎么优化”，你可以答：

> 可以进一步做 chunk 级去重、文档版本管理，或者引入更细粒度的哈希和相似内容检测机制。

---

## 4. 在线流程如何实现

### 4.1 在线流程总览

在线问答入口在 `app_qa.py`。

流程如下：

1. 用户在聊天框输入问题。
2. 前端调用 `RagService().chain.stream({"input": prompt}, config.session_config)`。
3. `rag.py` 中先把问题送给 retriever 做向量检索。
4. 将召回文档格式化成 `context`。
5. 再把 `input + context + history` 一起填入 prompt。
6. 调用 `ChatTongyi` 大模型生成回答。
7. 输出经过 `StrOutputParser()` 解析为普通字符串。
8. 回答会被写入历史消息，实现多轮对话。

### 4.2 在线链路在代码里怎么串起来

核心在 `rag.py:66-78`。

这段链式调用可以拆成 5 步理解：

1. `RunnablePassthrough()` 接收原始输入。
2. `RunnableLambda(format_for_retriever) | retriever | format_document`
   根据用户问题检索文档，并格式化成上下文文本。
3. `RunnableLambda(format_for_prompt_template)`
   把输入整理成 prompt 需要的结构：`input`、`context`、`history`。
4. `self.prompt_template | self.chat_model`
   用提示词模板组织消息后调用大模型。
5. `StrOutputParser()`
   把模型输出解析成字符串。

面试时你可以说：

> 我的在线链路不是简单地把用户问题直接发给模型，而是先检索知识，再把检索结果和历史消息共同注入 prompt，最后让模型基于增强上下文生成答案。

### 4.3 检索是怎么做的

检索封装在 `vector_stores.py` 里。

`VectorStoreService.get_retriever()` 调用：

```python
self.vector_store.as_retriever(search_kwargs={"k": config.similarity_threshold})
```

这里实际控制的是 top-k 返回条数，当前配置是 `config_data.py:17`，值为 `1`。

这意味着：

1. 每次只取 1 条最相关 chunk。
2. 实现简单，但召回覆盖面比较弱。

面试官如果问“这有什么问题”，你要会答：

> 当前 top-k 只取 1，适合演示，但真实业务里往往会取 3 到 5 条，或者增加重排步骤，否则容易因为单条召回不准导致最终回答不稳定。

### 4.4 prompt 中注入了哪些信息

在 `rag.py:27-35` 中，prompt 由三部分组成：

1. system 提示词
2. `MessagesPlaceholder("history")` 注入历史消息
3. user 当前问题

此外，检索出来的 `context` 也会作为变量填入 prompt。

所以这个项目不是单轮 QA，而是“带历史消息的 RAG 多轮问答”。

---

## 5. 记忆功能如何实现

这是你面试时很可能被重点追问的部分。

### 5.1 记忆的本质

这个项目的“记忆”不是向量记忆，也不是摘要记忆，而是“文件持久化的会话历史记忆”。

它的核心目的是让模型在下一轮回答时，能看到之前几轮对话内容。

### 5.2 具体实现方式

关键代码在：

- `rag.py:73-78`
- `file_history_store.py:8-9`
- `file_history_store.py:12-53`

实现步骤如下：

1. 在 `rag.py` 中使用 `RunnableWithMessageHistory` 对原始 chain 进行包装。
2. 这个包装器会通过 `get_history(session_id)` 获取某个会话对应的历史存储对象。
3. `get_history(session_id)` 返回 `FileChatMessageHistory(session_id, "./chat_history")`。
4. `FileChatMessageHistory` 会把每个 `session_id` 的聊天记录保存到 `./chat_history/session_id` 对应的文件里。
5. 每轮对话结束后，新消息会追加保存。
6. 下一轮问答时，再从该文件中读取历史消息并恢复为 LangChain 的消息对象。

### 5.3 历史消息是如何落盘的

`file_history_store.py` 里做了两件事：

1. `add_messages()` 中把消息对象转成字典，再写入 JSON。
2. `messages` 属性中从 JSON 读取，再恢复成消息对象列表。

具体看：

- 写入：`file_history_store.py:22-39`
- 读取：`file_history_store.py:41-49`

这说明它的存储机制是：

> 内存里的消息对象 <-> JSON 字典 <-> 本地文件

### 5.4 `session_id` 起了什么作用

`session_id` 是记忆隔离的关键。

在 `rag.py:85-88` 和 `config_data.py:22-25` 中，项目传入了：

```python
{
    "configurable": {
        "session_id": "user_001"
    }
}
```

含义是：

1. 不同用户或不同会话可以绑定不同的 `session_id`。
2. 每个 `session_id` 对应单独的历史文件。
3. 这样就能实现多会话隔离，避免 A 用户看到 B 用户的聊天上下文。

### 5.5 这个记忆方案的优点

1. 实现简单，容易讲清楚。
2. 能持久化，不会因为程序重启而丢失历史。
3. 便于本地调试和查看历史内容。
4. 很适合教学项目和中小型 demo。

### 5.6 这个记忆方案的局限

这是面试里最容易拉开差距的地方。

局限包括：

1. 它是“原始历史全量拼接”，没有做历史裁剪。
2. 对话越来越长时，prompt token 会持续增长。
3. 文件存储不适合高并发场景。
4. 没有做过期管理、摘要压缩和权限控制。
5. 当前 `session_id` 在配置里写死成 `user_001`，真实系统中应该动态生成。

如果面试官问“怎么优化记忆功能”，你可以答：

> 我会从三个方向优化：第一是做窗口记忆或摘要记忆，避免上下文无限膨胀；第二是把文件存储改成 Redis 或数据库，支持并发和过期控制；第三是把 session_id 改成用户维度动态管理，保证会话隔离和可扩展性。

---

## 6. 面试高频问题清单

下面这些问题，你大概率会被问到。

### 6.1 这个项目为什么要做 RAG，而不是直接问大模型

参考回答：

> 因为通用大模型不掌握我的私有知识，而且容易产生幻觉。RAG 的核心价值是先从私有知识库检索相关内容，再把检索结果喂给模型，这样答案会更贴近企业私域数据，也更可控。

### 6.2 你的项目里 RAG 的核心链路是什么

参考回答：

> 核心链路分成离线和在线两部分。离线部分负责把文档切分、向量化并写入 Chroma；在线部分负责根据用户问题检索相关 chunk，把 chunk、用户问题、历史消息一起组装进 prompt，再交给大模型生成答案。

### 6.3 记忆功能是怎么实现的

参考回答：

> 记忆功能是通过 LangChain 的 `RunnableWithMessageHistory` 实现的。每次请求会根据 `session_id` 获取对应的历史存储对象，历史消息会序列化成 JSON 存到本地文件里。下一轮提问时，再把历史消息读取出来并注入 prompt，从而实现多轮对话记忆。

### 6.4 离线流程是怎么实现的

参考回答：

> 离线流程入口是文件上传。上传后先读取文本，然后做 MD5 去重；如果内容较长就用递归切分器切成多个 chunk，再用 DashScope 的 embedding 模型生成向量，最后把文本块和元数据写入 Chroma 持久化目录。

### 6.5 为什么要做 chunk overlap

参考回答：

> 因为如果严格按边界切分，关键信息可能被截断到相邻两个 chunk 里，导致单个 chunk 语义不完整。加 overlap 能减少边界信息损失，提高召回质量。

### 6.6 你这个项目是怎么避免重复入库的

参考回答：

> 我在文档入库前对整段文本计算 MD5，把哈希值写入本地文件。下次上传相同内容时先查重，如果 MD5 已存在就跳过入库，避免重复 embedding 和重复写向量库。

### 6.7 你这个项目的检索策略是什么

参考回答：

> 目前是最基础的向量相似度检索，用 Chroma 的 retriever 来做 top-k 召回。当前配置只返回 1 条结果，适合教学演示，但在真实项目里我会把 top-k 调大，并考虑增加 rerank 或混合检索。

### 6.8 这个项目里有哪些可以优化的点

参考回答：

> 可以从四个方向优化：第一，检索层增加 top-k、重排和混合检索；第二，记忆层改成窗口记忆或摘要记忆，并接入 Redis；第三，知识库层支持更多文件格式和增量更新；第四，工程层增加异常处理、权限控制和监控日志。

### 6.9 如果检索不到内容怎么办

参考回答：

> 当前代码里会返回一个类似“未检索到相关内容”的兜底上下文，然后模型仍然会回答。更稳妥的做法是增加一个明确拒答策略，比如当召回分数低于阈值时，提示用户知识库中暂无答案，而不是让模型自由发挥。

### 6.10 RAG 一定能解决幻觉吗

参考回答：

> 不能完全消除，只能显著降低。因为最终回答仍然是大模型生成的，如果检索错了、召回不全、prompt 设计不合理，模型还是可能幻觉。所以 RAG 的关键不只是“接了向量库”，而是检索质量、上下文组织和回答约束一起做好。

---

## 7. 你可以重点强调的项目亮点

如果面试官让你讲“你这个项目的亮点是什么”，你可以从下面几个角度答。

### 7.1 亮点一：离线和在线链路完整

这个项目不是只写了一个聊天 demo，而是把知识入库、向量存储、检索增强生成、多轮记忆串成了完整闭环。

### 7.2 亮点二：有持久化能力

向量库持久化到 `./chroma_db`，聊天历史持久化到 `./chat_history`，程序重启后数据仍然存在。

### 7.3 亮点三：考虑了去重

虽然是基础版实现，但已经有文档 MD5 去重，说明你不是单纯跑通流程，而是考虑到了重复数据问题。

### 7.4 亮点四：代码结构清晰

知识库、向量库、问答链、历史存储、前端页面分别拆分，职责边界比较明确，便于后续扩展。

---

## 8. 你也要主动承认的不足

面试里不是只讲优点，敢讲不足反而更像真正做过项目的人。

你可以主动说：

1. 当前只支持 txt 文件，文件类型支持比较单一。
2. 检索只做了基础向量召回，没有 rerank。
3. top-k 只取 1，召回稳定性一般。
4. 记忆存储是本地文件，不适合生产环境。
5. 没有做 chunk 命中展示和引用高亮。
6. 没有做召回分数阈值控制和严格拒答。
7. `session_id` 是写死配置，真实环境应动态化。

这类回答会很加分，因为它体现了你有工程视角。

---

## 9. 如果面试官让你讲“项目难点”

你可以这样组织：

> 这个项目虽然代码量不算很大，但真正的难点不在于把 LangChain API 调通，而在于把离线入库、在线检索、历史记忆这三块串成一个稳定闭环。比如离线阶段要考虑切分粒度和去重；在线阶段要考虑检索结果怎么组织进 prompt；多轮问答还要考虑 session 级别的历史隔离。这几个点如果没有串起来，项目就只是一个简单的单轮问答 demo。

---

## 10. 一段适合直接背诵的项目介绍

你可以准备下面这段 1 分钟版本：

> 我做的是一个基于 LangChain 的 RAG 问答项目，底层用了 DashScope 的 embedding 模型、Chroma 向量库和通义千问大模型。项目分成离线和在线两条流程。离线流程中，用户上传 txt 文档后，我会先做 MD5 去重，再用递归切分器把长文本切成 chunk，生成向量后持久化到 Chroma。在线流程中，用户提问会先做向量检索，再把召回的知识片段、当前问题和历史对话一起组装进 prompt，最后调用大模型生成答案。项目还通过 `RunnableWithMessageHistory` 实现了基于 `session_id` 的多轮记忆，并把历史消息落盘到本地文件。这个项目的价值在于搭建了一个完整的 RAG 闭环，同时也让我理解了知识入库、检索增强和会话记忆在实际系统中的协作方式。

---

## 11. 一段适合回答“记忆功能如何实现”的标准话术

> 这个项目里的记忆功能，本质上是会话历史记忆，而不是向量记忆。实现上我使用了 LangChain 的 `RunnableWithMessageHistory`，它会在每次请求时根据 `session_id` 获取对应的历史消息对象。历史消息通过自定义的 `FileChatMessageHistory` 持久化到本地文件中，写入时先把消息对象序列化成字典，再保存成 JSON；读取时再从 JSON 恢复成消息对象。这样下一轮提问时，历史消息就能通过 `MessagesPlaceholder("history")` 注入到 prompt 中，模型因此具备多轮对话上下文。这个方案实现简单、适合 demo，但如果是生产环境，我会进一步改造成 Redis 或数据库存储，并增加窗口裁剪或摘要记忆来控制 token 成本。

---

## 12. 一段适合回答“离线流程如何实现”的标准话术

> 离线流程就是知识入库流程。用户上传文本文件后，系统先读取原始文本并计算 MD5 做去重；如果文本长度超过阈值，就使用 `RecursiveCharacterTextSplitter` 按设定的 chunk size、overlap 和分隔符切成多个文本块；然后通过 DashScope Embedding 模型把这些 chunk 向量化，并将文本、向量和元数据一起写入 Chroma 持久化向量库。这样在线问答时，用户问题就可以先去 Chroma 检索相关知识片段，再交给大模型生成更可靠的回答。

---

## 13. 针对你这个项目，面试官可能继续深挖的问题

你最好提前准备这些追问：

1. 为什么选择 Chroma，而不是 FAISS 或 Milvus？
2. 为什么 chunk size 设成 1000？怎么调优？
3. overlap 设 100 的依据是什么？
4. 为什么 top-k 只取 1？如果召回错了怎么办？
5. 如果知识库很大，检索性能如何保证？
6. 如果用户上传的是 pdf、docx，怎么扩展？
7. 历史对话越来越长，token 爆炸怎么处理？
8. 怎么做来源引用和答案可追溯？
9. 怎么降低模型基于错误上下文的幻觉？
10. 如果要上线成企业系统，你会怎么改架构？

---

## 14. 你现在最值得补强的面试表达

结合你这个项目，我建议你把下面 4 句话练熟：

1. “我的项目分成离线建库和在线问答两条主链路。”
2. “记忆功能本质上是基于 session_id 的会话历史持久化。”
3. “离线流程核心是去重、切分、向量化、持久化入库。”
4. “当前版本是教学型 Demo，生产化还需要补检索优化、记忆压缩和工程治理。”

只要这 4 句话你说顺了，面试官基本会觉得你是真理解了，而不是只照着教程敲过一遍。

---

## 15. 结合代码可以重点看的位置

复习时建议优先看下面这些文件：

1. `rag.py`
   看在线主链路如何把检索、prompt、模型、history 串起来。
2. `file_history_store.py`
   看记忆如何按 `session_id` 持久化。
3. `knowledge_base.py`
   看离线入库、切分、MD5 去重、Chroma 写入。
4. `vector_stores.py`
   看 retriever 是如何封装出来的。
5. `config_data.py`
   看 chunk、模型名、向量库目录、session 配置。

---

## 16. 最后给你的面试建议

你讲这个项目时，不要只讲“我用了 LangChain、Chroma、Qwen”。

更好的讲法是始终围绕这 3 个问题展开：

1. 知识是怎么进来的？
2. 用户提问后系统怎么找到知识并生成答案？
3. 多轮对话为什么能记住上文？

只要你能把这三件事讲清楚，这个项目在初级到中级面试里就已经够用了。
